
ThinkGear Stream Parser
Default (windows + osx). TG_STREAM_PARSER in setup().

ThinkGear Communications Driver (osx compatible only at the moment)
When using the ThinkGear Communications Driver (osx compatible only at the moment). Place the ThinkGear.bundle in the bin/data folder. TG_COMMS_DRIVER in setup().